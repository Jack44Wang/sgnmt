"""model.py defines the Reinforcement Learning agent, which is going to take
the hidden states in the Transformer simultaneous translation scheme and returns
the predicted rewards for each of the actions (READ, WRITE).
"""
